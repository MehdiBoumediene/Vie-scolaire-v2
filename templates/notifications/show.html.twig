{% extends 'base.html.twig' %}

{% block title %}Notifications{% endblock %}

{% block body %}
    <h1>Notifications</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notification.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ notification.type }}</td>
            </tr>
            <tr>
                <th>Classeid</th>
                <td>{{ notification.classeid }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ notification.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notifications_index') }}">back to list</a>

    <a href="{{ path('app_notifications_edit', {'id': notification.id}) }}">edit</a>

    {{ include('notifications/_delete_form.html.twig') }}
{% endblock %}
