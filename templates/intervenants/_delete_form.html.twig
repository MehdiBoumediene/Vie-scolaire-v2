<form method="post" action="{{ path('app_intervenants_delete', {'id': intervenant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display:contents;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intervenant.id) }}">
    <button class="mb-2 mr-2 btn-outline-2x btn btn-shadow-danger btn-outline-danger" style="float:right;">Archiver</button>
</form>
