<form method="post" action="{{ path('app_entreprises_delete', {'id': entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display:contents;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entreprise.id) }}">
    <button class="mb-2 mr-2 btn-outline-2x btn btn-shadow-danger btn-outline-danger" style="float:right;">Supprimer</button>
</form>
